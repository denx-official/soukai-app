<% provide(:title, "ログイン") %>
<h1>ログイン</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:session, url: login_path) do |f| %>

      <%= f.label :email %>
      <%= f.text_field :email, class: 'form-control' %>
      <%= f.select :domain, options_for_select(@domain, include_blank: '-- 未指定 --'),
          {}, class: 'form-control' %>
      
      <%= f.label :password %>
      <%= link_to "(パスワードを忘れた)", new_password_reset_path %>
      <%= f.password_field :password, class: 'form-control' %>
      
      <%= f.label :remember_me, class: "checkbox inline" do %>
        <%= f.check_box :remember_me %>
        <span class="checkbox-icon">ログインを保持</span>
      <% end %>
      
      <%= f.submit "Log in", class: "btn btn-primary" %>
    <% end %>

    <p>まだ登録していない人は登録してください <%= link_to "登録ページ", signup_path %></p>
    <p><%= link_to "テキストボックスでログイン", login_path %></p>
  </div>
</div>